<scroll-view class="page" scroll-y enable-flex>
  <view class="hero card">
    <view class="hero-title">{{projectName}}</view>
    <view class="hero-subtitle">{{subtitle}}</view>
    <view class="hero-time">最近更新：{{updateTime}}</view>
    <view class="hero-hint">{{connectionHint}}</view>
  </view>

  <view class="section-title split-title">多设备接入</view>
  <scroll-view class="device-scroll" scroll-x>
    <view class="device-chip-wrap">
      <block wx:for="{{devices}}" wx:key="id">
        <view
          class="device-chip {{index === currentDeviceIndex ? 'active' : ''}}"
          data-index="{{index}}"
          bindtap="selectDevice"
        >
          <text class="chip-name">{{item.name}}</text>
          <text class="chip-id">{{item.id}}</text>
        </view>
      </block>
    </view>
  </scroll-view>

  <view class="card section">
    <view class="section-title">当前设备信息</view>
    <view class="device-grid">
      <view class="item">
        <text class="label">设备编号</text>
        <text class="value">{{device.id}}</text>
      </view>
      <view class="item">
        <text class="label">部署位置</text>
        <text class="value">{{device.location}}</text>
      </view>
      <view class="item">
        <text class="label">在线状态</text>
        <text class="value status {{device.status === '在线' ? 'online' : 'offline'}}">{{device.status}}</text>
      </view>
      <view class="item">
        <text class="label">信号质量</text>
        <text class="value">{{device.signal}}</text>
      </view>
      <view class="item">
        <text class="label">电量</text>
        <text class="value">{{device.battery}}%</text>
      </view>
    </view>
  </view>

  <view class="section-title split-title">水质核心指标（点击查看图表）</view>
  <view class="metrics-wrap">
    <block wx:for="{{metrics}}" wx:key="key">
      <view
        class="card metric-card"
        bindtap="goMetricDetail"
        data-key="{{item.key}}"
        data-title="{{item.title}}"
        data-unit="{{item.unit}}"
        data-value="{{item.value}}"
      >
        <view class="metric-head">
          <text class="metric-name">{{item.title}}</text>
          <text class="metric-level">{{item.level}}</text>
        </view>
        <view class="metric-value-row">
          <text class="metric-value">{{item.value}}</text>
          <text class="metric-unit">{{item.unit}}</text>
        </view>
        <view class="metric-desc">{{item.desc}}</view>
        <view class="metric-trend">{{item.trend}}</view>
      </view>
    </block>
  </view>

  <view class="card section">
    <view class="section-title">功能入口</view>
    <view class="quick-list">
      <block wx:for="{{quickActions}}" wx:key="key">
        <view class="quick-item" data-key="{{item.key}}" bindtap="goQuickAction">
          <view class="quick-title">{{item.title}}</view>
          <view class="quick-tip">{{item.tip}}</view>
        </view>
      </block>
    </view>
  </view>
</scroll-view>
